# Atlas 整理 Agent · 测试清单

针对当前画布（9 张卡）验证「AI 整理」通道。核心纪律贯穿每条：
**Agent 只动布局与关系，绝不改卡片正文（名称/简介/时间）。**

## 测试用的画布快照

| id 前缀 | 类型 | 名称 |
|---|---|---|
| rule | 规则 | 规则 |
| org | 组织 | 组织 1 |
| org | 组织 | 组织 2 |
| character | 角色 | 角色 1 |
| character | 角色 | 角色 2 |
| map | 地图 | 地图 |
| location | 地点 | 地点 1 |
| location | 地点 | 地点 2 |
| event | 事件 | 事件（默认阶段：发展，无日期） |

操作前先在命令条切到「整理」模式再输入。

---

## A. 布局整理（arrange_canvas）

**T1 · 按类型归类**
输入：`帮我按类型把画布归类整理一下`
期望：调用 `arrange_canvas(by_type)` → 组织/角色/地点各成一列，规则、地图、事件各自归列。
通过：预览里 9 张卡都有幽灵新位、同类同列；采纳后正文一字未变。

**T2 · 摆整齐（网格）**
输入：`太乱了，把所有卡片摆整齐`
期望：`arrange_canvas(grid)`，近似正方形网格。
通过：卡片对齐成网格，无重叠。

**T3 · 以关系为中心成环**
前置：先做 T6/T7 建几条关系。
输入：`把关系最多的对象放中间，其它围成一圈`
期望：`arrange_canvas(radial)`，度数最高的对象居中，其余环绕。
通过：中心对象确为连线最多的那张。

**T4 · 时间线雏形**
输入：`按时间线排一下`
期望：`arrange_canvas(timeline)`。当前只有 1 个事件，应落在「发展」阶段列。
通过：事件卡移到时间线位置；只有 1 个事件时不报错（可能只移动一张，属正常）。
补充：可先给「事件」在详情卡设阶段/日期，再测多事件的泳道错开。

---

## B. 聚拢一组（cluster_cards）

**T5 · 指名聚拢**
输入：`把组织 1、组织 2、角色 1 聚到一块`
期望：`cluster_cards(ids=[org1,org2,character1])` → 三张卡聚成紧凑网格。
通过：只有这三张移动，其余不动；Agent 用的是 id 不是名字。

---

## C. 关系整理（link_objects）—— 本页重点

**T6 · 结构性关系**
输入：`把角色 1 归到组织 1 名下`
期望：`link_objects(source=character1, target=org1, relation=隶属)`。
通过：预览出现虚线连接；采纳后切到「关系」投影能看到连线 + 中点 chip。

**T7 · 复杂关系（带叙事）**
输入：`角色 1 和角色 2 是死对头，早年因为一件事结下的仇`
期望：`link_objects(source=character1, target=character2, relation=敌对, note=…)`。
通过：采纳后点开关系详情卡，`note` 落在「这段关系的故事」里；连线中点 chip 显示段落图标。**两张角色卡的正文没被写任何字。**

**T8 · 一句话多关系**
输入：`地点 1 属于组织 1，事件发生在地点 2`
期望：两次 `link_objects`（地点1→组织1；事件→地点2）。
通过：预览列出 2 条连接，计数显示「连接 2」。

**T9 · 移除关系（unlink）**
前置：先建一条关系。
输入：`把角色 1 和角色 2 之间的关系去掉`
期望：`unlink(relation_id=…)`，Agent 从快照里认出那条关系 id。
通过：采纳后该连线消失。

---

## D. 边界与纪律（必须守住）

**T10 · 拒绝改正文**
输入：`把角色 1 改名叫艾琳娜，再给组织 1 写一段公开简介`
期望：Agent **不产生任何内容编辑**（工具集里根本没有写正文的函数）。可能返回空方案、或只做无关的布局动作。
通过：角色 1 名称仍是「角色 1」，组织 1 简介仍为空。若返回空方案，命令条提示「没找到可整理的动作」属正常。

**T11 · 采纳 / 撤销一致性**
步骤：任一条产生预览 → 先点「撤销」→ 再复跑 → 点「采纳」。
通过：撤销后画布与预览前完全一致（幽灵消失、无副作用）；采纳后动作全部落地。

**T12 · 空输入 / 乱码**
输入：空字符串、或 `asdfghjkl`。
通过：空输入不触发请求；乱码返回空方案并友好提示，不崩、不乱动画布。

---

## E. 接入健壮性

**T13 · 无 Key 兜底**（把 `AgentSecrets` 的 key 临时清空再测）
期望：退回 `MockOrganizer` 启发式，「整理/按类型/时间线/两个对象名」等仍能出方案，面板标注「本地演示 · 未配置 DeepSeek Key」。

**T14 · 网络/鉴权失败**（故意填错 key 或断网）
期望：命令条出现红字错误（如「DeepSeek 请求失败（401）…」），不崩溃，可重试。

---

## 判定总则
1. 每条采纳后，**所有卡片的名称/简介/时间保持不变**——这是硬红线。
2. Agent 引用对象/关系一律用 id，不因「组织 1 / 组织 2」这类雷同名字搞混。
3. 预览（幽灵卡 + 虚线）必须先于落地；作者不点采纳就不改画布。
